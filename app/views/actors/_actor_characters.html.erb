<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Characters

    <a data-toggle="collapse" href="#actor_characters_new_form">
      Add casting
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @actor.characters.each do |casting| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to casting.to_s, casting_path(casting) %>

        <%= link_to casting, method: :delete, data: { confirm: "Do you really want to delete this casting" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="actor_characters_new_form">
        <%= render partial: "actors/actor_characters_new_form" %>
      </div>
    </div>

  </ul>
</div>
